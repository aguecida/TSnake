namespace Constants {
    export const blockSize: number = 10;
    export const fps: number = 50;
    export const canvasColor: string = '#555';
    export const canvasHeight: number = 400;
    export const canvasWidth: number = 600;
}
